from setuptools import setup

install_requires = [
]

packages = [
    'private_utils',
]

console_scripts = [
]


setup(
    name='private_utils',
    version='0.0.3',
    packages=packages,
    install_requires=install_requires,
)
